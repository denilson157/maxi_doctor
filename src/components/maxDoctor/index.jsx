import React from 'react'
import { Header, Form, Actions } from './components'

const MaxDoctor = () =>
(
    <div>
        <Header />
        <Form />
        <Actions />
    </div>
);

export default MaxDoctor;
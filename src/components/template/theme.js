import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#774a9a',
        },
        secondary: {
            main: '#8b6eae',
        },
        background: {
            default: '#e8e1f0',
        }
    },
});

export default theme;
